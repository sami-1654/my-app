# خدمة تحميل الأغاني من يوتيوب — للنشر على Railway

## الخطوات

1. روح لـ https://railway.app وسجّل دخول
2. اضغط **New Project** → **Deploy from GitHub repo** (لو رفعت هذا المجلد
   لـ GitHub أولاً) أو **Empty Project** ثم ارفع الملفات يدوياً عبر Railway CLI
3. بعد إنشاء المشروع، روح لتبويب **Variables** وأضيفي متغير بيئة جديد:
   - الاسم: `SERVICE_API_KEY`
   - القيمة: أي نص سري طويل وعشوائي تختارينه بنفسك (مثال:
     `xK9mP2vL8qR5tY7wZ3nB1cF6hJ4dS0`) — احتفظي بنفس القيمة، بتحتاجينها
     بالخطوة الثانية (إعداد بوت تيليجرام)
4. Railway بيكتشف `Dockerfile` تلقائياً ويبني الخدمة (يثبّت ffmpeg + المكتبات)
5. بعد نجاح النشر، روح لتبويب **Settings** → **Networking** → فعّلي
   **Generate Domain** عشان يعطيك رابط عام (مثلاً:
   `https://your-service.up.railway.app`)
6. اختبري إنها شغالة: افتحي بالمتصفح
   `https://your-service.up.railway.app/health` — المفروض يرجع
   `{"status": "ok"}`

## بعد النشر

ارجعي لي برابط الخدمة (`https://your-service.up.railway.app`) ومفتاح
`SERVICE_API_KEY` اللي اخترتيه، وبربطهم ببوت تيليجرام مباشرة.

## ملاحظات مهمة

- الخطة المجانية بـ Railway عندها حد استخدام شهري (ساعات تشغيل + استهلاك
  شبكة) — لو المجموعة نشيطة جداً بطلب الأغاني ممكن تحتاجي ترقّي لخطة مدفوعة
  رخيصة لاحقاً
- تحميل صوتيات من يوتيوب قد يخالف شروط استخدام يوتيوب — هذا استخدام على
  مسؤوليتكم الشخصية
- لو يوتيوب غيّر آلية الحماية لديه (يصير أحياناً) قد تحتاج تحديث مكتبة
  `yt-dlp` لأحدث إصدار (`pip install -U yt-dlp` بملف requirements.txt)
